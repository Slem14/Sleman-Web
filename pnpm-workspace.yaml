packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts by default (supply-chain control).
# Only explicitly trusted packages may run them — extend deliberately, never broadly.
onlyBuiltDependencies:
  - esbuild
  - sharp
